let initialState = {
    friends: [
        { id: 1, name: "Sasha" },
        { id: 2, name: "Serezha" },
        { id: 3, name: "Stephan" }
    ]
}

const sidebarReducer = (state = initialState, action) => {

    return state;
}

export default sidebarReducer;